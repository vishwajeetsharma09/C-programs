#include <stdio.h> #include<string.h> #include<stdlib.h> #define max 100      int top=-1;     char stack_string[max];// stack which hold 100 characters   void pushChar(char item)  { /*check for full*/       if(top==max-1)      {       printf("\nStack is FULL !!!\n");        return;       } /*increase top and push item in stack*/ top=top+1;      stack_string[top]=item;  }  char popChar()  {      char item;     if(top==-1) {         printf("\nStack is EMPTY!!!\n");         return 0;       } /*pop item and decrease top*/      item = stack_string[top];      top=top-1;     return item;  }  int main() {    
